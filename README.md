# neural-c

A neural network library in pure C.

## Install

copy include/neural.h C:\mingw64\include
copy lib/libneural.a C:\mingw64\lib

## Usage

#include <neural.h>

## Build

gcc <FILE NAME>.c -lneural -lm <File Name>